import React, { useState } from 'react';
import { Stack } from '@mui/material';

const CarReview = () => {
	return (
		<div className={'car-review'}>
			<Stack className={'container'}>CAR REVIEW</Stack>
		</div>
	);
};

export default CarReview;
