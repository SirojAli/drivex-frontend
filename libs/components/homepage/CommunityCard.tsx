import React, { useState } from 'react';
import { Stack, Box } from '@mui/material';

const CommunityCard = ({ initialInput, ...props }: any) => {
	return (
		<Stack className={'community-board-card'}>
			<span>CAR BLOG CARD</span>
		</Stack>
	);
};

export default CommunityCard;
