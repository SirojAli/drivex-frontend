import React, { useState } from 'react';
import { Stack, Box } from '@mui/material';

const CarReviewCard = ({ initialInput, ...props }: any) => {
	return (
		<Stack className={'car-review-card'}>
			<span>CAR REVIEW CARD</span>
		</Stack>
	);
};

export default CarReviewCard;
